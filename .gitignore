# Dependencias
node_modules
.pnp
.pnp.js

# Build
dist
dist-ssr
*.local

# TypeScript build cache
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Entorno (Supabase, etc.)
.env
.env.local
.env.*.local

# Claude Code — settings locales/permisos de la máquina (no compartir).
# settings.json lo escribe `graphify claude install` con rutas absolutas al
# Python de esta máquina: si se sube, se rompe en cualquier otra.
.claude/settings.local.json
.claude/settings.json

# Grafo de conocimiento de graphify (se regenera con `graphify update .`)
graphify-out/

# Fotos fuente de la quinta (pesadas, con video) — al repo van solo las
# optimizadas de public/img/quinta/
/Quinta/
